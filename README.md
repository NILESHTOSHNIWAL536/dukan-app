# Smart Dalal API

Run:
```bash
npm install
cp .env.example .env
npm run dev
```

On Windows PowerShell, create `.env` by copying `.env.example` manually if `cp` is unavailable.

MongoDB must be running locally or `MONGODB_URI` must point to a reachable MongoDB instance.
